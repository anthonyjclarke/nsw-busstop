#pragma once

// ---------------------------------------------------------------------------
// secrets.h — copy this file to secrets.h and fill in your values.
// secrets.h is gitignored — never commit it.
// ---------------------------------------------------------------------------

#define SECRET_WIFI_SSID    ""   // Optional: seed WiFiManager captive portal
#define SECRET_WIFI_PASS    ""   // Optional: seed WiFiManager captive portal

// NAS API authentication — required if server has AUTH_ENABLED=true
// Leave empty if AUTH_ENABLED=false on the server
#define SECRET_NAS_API_KEY  ""   // Bearer token for NAS server /api/state
