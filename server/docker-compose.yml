services:
  nsw-busstop-server:
    build:
      context: .
    container_name: nsw-busstop-server
    env_file:
      - ./app/.env
    ports:
      - "8081:8081"
    volumes:
      - nsw-busstop-data:/app/data
    restart: unless-stopped

volumes:
  nsw-busstop-data:
